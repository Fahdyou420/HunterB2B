services:
  hunter_auto:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./config:/app/config
      - playwright_sessions:/app/sessions
      - ./logs:/app/logs
    env_file:
      - .env

volumes:
  playwright_sessions:
